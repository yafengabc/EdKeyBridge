# Build output
edkeybridge.exe
edkeybridge_*.exe
psil.exe
psil_*.exe

# Runtime config & self-test artifacts (per-user, not source)
edkeybridge.toml
edkeybridge_selftest.log
edkeybridge_selftest.toml

# Project data / editor state
.workbuddy/

# OS noise
Thumbs.db
Desktop.ini

# NOTE: src/rsrc_windows_amd64.syso is intentionally tracked — it embeds the
# comctl32 v6 manifest + app icon so a plain `go build` yields a themed exe.
# It must live next to the sources: Go only links .syso files in the package dir.
