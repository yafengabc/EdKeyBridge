# 更新日志

EdKeyBridge 的重要变更都记在这里。
（English version: [CHANGELOG.md](CHANGELOG.md)）

## [v0.1.1] - 2026-09-16

### 新增

- **系统托盘图标** —— 程序最小化后收进托盘（同时去掉任务栏按钮）；单击/双击托盘图标恢复窗口，
  右键菜单可直接「打开窗口 / 开始·停止 / 退出」。首次最小化会弹气泡提示去哪儿找图标。
- **权限（完整性级别）自检** —— 标题栏显示本程序权限，状态栏显示前台窗口权限。
  当游戏权限高于本程序（UIPI 会静默丢弃注入的按键）时给出告警。
- **自动 UAC 提权** —— 检测到前台窗口权限更高时，询问是否以管理员身份重启。
  询问前先把本程序窗口拉到前台；若全屏游戏仍把对话框埋住，会临时最小化游戏并在事后还原。
- **提权重启后无缝续接** —— 重启后的实例无条件自动继续桥接（`-resume`），不受 `autostart`
  配置影响；任务栏持续闪烁直到你点开它（新窗口否则也藏在游戏后面）。
- **退出时自动保存配置** —— 之前只有点「保存配置」和提权前会写 `edkeybridge.toml`，
  改了设置直接关窗口就丢了。
- 启动日志会打印配置文件路径。

### 修复

- 复选框点不动（`BS_CHECKBOX` 不会自动切换，改用 `BS_AUTOCHECKBOX`）。
- 日志不换行：Go 的 `log` 输出裸 LF，而 Windows EDIT 控件只认 CRLF。
- 状态栏中文乱码（误用 `SB_SETTEXTA`，改用 `SB_SETTEXTW`）。
- `ShellExecuteW(runas)` 未指定工作目录，导致提权后的实例工作目录是 `System32`。
- 提权重启不再依赖保存的 `autostart` 值——否则可能出现"刚提完权却停在已停止"。
- 提权失败时日志记录 `ShellExecute` 返回码（5 = UAC 被取消/被策略拦，2/3 = exe 路径无效），
  不再是无声失败。

### 变更

- 默认启动即开始桥接（`autostart = true`）。
- 运行状态与前台窗口移到底部状态栏，省下的空间全给日志。
- 界面调整：英文界面用 Tahoma 9pt，「最小按下时长」独立成行，标签与输入框垂直对齐，
  日志区撑满剩余空间。

## [v0.1.0] - 2026-09-15

- 首个版本：Win32 GUI、把 RustDesk 安卓端发往 Windows 的按键转成《精英危险》能响应的真实按键、
  中英双语（自动检测）、TOML 配置、内嵌图标与清单。
